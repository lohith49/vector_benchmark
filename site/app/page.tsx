import { loadReport } from "../lib/data";
import { BuildTimeChart } from "./components/BuildTimeChart";
import { DatabaseCard } from "./components/DatabaseCard";
import { DatasetSwitch } from "./components/DatasetSwitch";
import { Hero } from "./components/Hero";
import { KpiStrip } from "./components/KpiStrip";
import { LatencyChart } from "./components/LatencyChart";
import { Navbar } from "./components/Navbar";
import { Recommendation } from "./components/Recommendation";
import { ResultsTable } from "./components/ResultsTable";
import { RunMetaPanel } from "./components/RunMetaPanel";

export default function Page() {
  const report = loadReport();
  const { narrative, results, run } = report;
  const dbs = Object.keys(narrative.per_database);
  const generatedAt = new Date(report.generated_at).toLocaleString();

  return (
    <>
      <Navbar runId={run.id} />
      <main>
        <Hero report={report} />

        {/* KPI winner strip */}
        <section id="kpis" className="container section">
          <div className="section-head">
            <span className="badge">Leaderboard</span>
            <h2 style={{ marginRight: "auto" }}>Who wins what</h2>
            <span className="subtle" style={{ fontSize: 13 }}>
              Per-metric leader across the run
            </span>
          </div>
          <p className="muted" style={{ marginBottom: 18, maxWidth: 720 }}>
            The fastest tail latency, highest recall, peak query throughput, and ingest rate — pulled
            directly from the result rows. Use this to triage the deep-dive sections below.
          </p>
          <KpiStrip results={results} />
        </section>

        {/* TL;DR */}
        <section className="container section">
          <div className="card" style={{ padding: 28 }}>
            <div style={{ display: "flex", gap: 8, alignItems: "center", marginBottom: 14, flexWrap: "wrap" }}>
              <span className="pill"><span className="swatch" style={{ color: "var(--green)" }} /> TL;DR</span>
              <span className="subtle" style={{ fontSize: 13 }}>Generated by {report.model} on {generatedAt}</span>
            </div>
            <ul className="bullet-list" style={{ fontSize: 16 }}>
              {narrative.tl_dr.map((b, i) => <li key={i}>{b}</li>)}
            </ul>
          </div>
        </section>

        {/* Run config panel */}
        <section className="container section">
          <RunMetaPanel report={report} />
        </section>

        {/* Pareto chart per dataset */}
        <section id="results" className="container section">
          <div className="section-head">
            <span className="badge">Quality vs. speed</span>
            <h2>Recall vs. p99 latency</h2>
          </div>
          <p className="muted" style={{ marginBottom: 20, maxWidth: 720 }}>
            The fundamental tradeoff in approximate nearest-neighbor search. We sweep ef_search and plot every operating point.
          </p>
          <div className="card">
            <DatasetSwitch results={results} variant="pareto" />
          </div>
        </section>

        {/* Latency bar chart */}
        <section className="container section">
          <div className="section-head">
            <span className="badge">Tail latency</span>
            <h2>p50 vs p99 at the highest ef_search</h2>
          </div>
          <p className="muted" style={{ marginBottom: 20, maxWidth: 720 }}>
            p50 (light) and p99 (solid) at the highest ef_search value tested per (database, dataset).
          </p>
          <div className="card">
            <LatencyChart results={results} />
          </div>
        </section>

        {/* Throughput chart */}
        <section id="throughput" className="container section">
          <div className="section-head">
            <span className="badge">Throughput</span>
            <h2>QPS vs recall</h2>
          </div>
          <p className="muted" style={{ marginBottom: 20, maxWidth: 720 }}>
            How many queries per second each engine sustains as you raise recall. Up-and-to-the-right is better.
          </p>
          <div className="card">
            <DatasetSwitch results={results} variant="throughput" />
          </div>
        </section>

        {/* Build time chart */}
        <section className="container section">
          <div className="section-head">
            <span className="badge">Index build</span>
            <h2>Insert + index time</h2>
          </div>
          <p className="muted" style={{ marginBottom: 20, maxWidth: 720 }}>
            Total time to ingest the dataset and build the HNSW index. Lower is better. Note that Weaviate indexes
            online, so its index time is effectively zero.
          </p>
          <div className="card">
            <BuildTimeChart results={results} />
          </div>
        </section>

        {/* Per-database breakdown */}
        <section id="per-database" className="container section">
          <div className="section-head">
            <span className="badge">Deep dive</span>
            <h2>Database breakdown</h2>
          </div>
          <p className="muted" style={{ marginBottom: 20, maxWidth: 720 }}>
            One card per engine. The medal reflects the average rank across recall, p50, and QPS.
          </p>
          <div className="grid" style={{ gridTemplateColumns: "repeat(auto-fit, minmax(320px, 1fr))" }}>
            {dbs.map(db => (
              <DatabaseCard key={db} db={db} info={narrative.per_database[db]} results={results} />
            ))}
          </div>
        </section>

        {/* Recommendation matrix */}
        <section className="container section">
          <div className="section-head">
            <span className="badge">Decision guide</span>
            <h2>Pick by use case</h2>
          </div>
          <Recommendation rec={narrative.recommendation} />
        </section>

        {/* Caveats */}
        <section className="container section">
          <div className="card" style={{ padding: 24 }}>
            <h3 style={{ marginBottom: 12 }}>Caveats</h3>
            <ul className="bullet-list weakness" style={{ fontSize: 14 }}>
              {narrative.caveats.map((c, i) => <li key={i}>{c}</li>)}
            </ul>
          </div>
        </section>

        {/* Raw table */}
        <section id="raw" className="container section">
          <div className="section-head">
            <span className="badge">Raw data</span>
            <h2>All result rows</h2>
          </div>
          <p className="muted" style={{ marginBottom: 16, maxWidth: 720 }}>
            One row per (database, dataset, ef_search). Click any column header to sort, use the chips to filter,
            export to CSV.
          </p>
          <ResultsTable results={results} />
        </section>

        {/* Footer */}
        <footer className="container" style={{ padding: "40px 0 80px" }}>
          <div className="card card-tight" style={{ display: "flex", justifyContent: "space-between", flexWrap: "wrap", gap: 16, alignItems: "center" }}>
            <div className="subtle" style={{ fontSize: 13 }}>
              Run <span className="kbd">{run.id.slice(0, 8)}</span> · {run.host} · {run.platform}
            </div>
            <div className="subtle" style={{ fontSize: 13 }}>
              Datasets from <a href="https://github.com/erikbern/ann-benchmarks" target="_blank" rel="noreferrer">ann-benchmarks</a>.
              Built with Next.js, Recharts, LangChain, Claude.
            </div>
          </div>
        </footer>
      </main>
    </>
  );
}
